export interface IUnits {
    wordNumbers?: any;
    nominalUnits?: any;
}
export declare type IGlobalUnit = any;
export default class ConversionsUtils {
    private units;
    private globalUnit;
    constructor(units: IUnits, globalUnit: IGlobalUnit);
    /**
     * Returns the key who has match the result
     */
    getUnitKey(unit: string): string | undefined;
    /**
     * Normalizes a given amount.
     * @param val The amount to be normalized.
     */
    normalizeAmount(val: number | string): number;
}
